// AutoMix — API base URL
// Change this to your Railway/backend URL when deploying the frontend separately.
// Example: "https://automix-api.up.railway.app"
// Leave empty ("") to use the same origin (when frontend is served by the backend).
window.AUTOMIX_API_BASE = "";
